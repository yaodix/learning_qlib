### 1. qlib_data的close价格与同花顺等不一致问题。

进行以下计算，价格将一致。

open=df["$open"] / df["$factor"],

high=df["$high"] / df["$factor"],

low=df["$low"] / df["$factor"],

close=df["$close"] / df["$factor"],

模型训练到底用前复权还是后复权数据



2. ETF数据有无复权问题
